notes: why 10 tweets?; why troll if >= 1 troll tweets. how many tweets?
c v may still have overfitting
define arbitrary points
To do:

write up **** 
1-2 figures, page of text, all data

Ensure that jeff can run the package:
1.) make sure that the emoji converting works
2.) make sure that the emoji dictionary function works
